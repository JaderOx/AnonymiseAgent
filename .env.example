# ════════════════════════════════════════════════════════════════
# Anonymise Agent — 环境变量配置模板
# 用法： cp .env.example .env  然后填入你的实际值
# 注意：输入/输出路径【不在这里配】，它们是命令行参数：
#       -i/--input_dir、-o/--output_dir、--middle_dir
# ════════════════════════════════════════════════════════════════

# ── 1. API Token ───────────────────────────────────────────────
# HuggingFace token：whisperx / pyannote 说话人分离需要
# 获取： https://huggingface.co/settings/tokens
# 首次使用需在 HuggingFace 上同意 pyannote/speaker-diarization-3.1 协议
# 不提供则跳过说话人分离（仅影响多说话人场景的 EER）
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxx

# ── 2. 模型存储目录（即“模型存储路径”）──────────────────────────
# LLM 模型目录（mask_llm 与 reflexion agent 使用）
# 目录下需有模型子目录： Qwen2.5-32B-Instruct/ 、 gemma-3-27b-it/
LLM_MODEL_DIR=./LLMs

# ASR / VC / age-gender 等模型的缓存根目录
# （运行时自动下载：paraformer、FunASR*、fish-speech、w2v2-L-robust-6-age-gender 等）
TORCH_HOME=./models

# ModelScope 模型下载缓存目录（FunASR 模型来源）
MODELSCOPE_CACHE=.

# ── 3. OpenAI 兼容 API（可选）──────────────────────────────────
# 仅当用 --no-open_source 让 reflexion agent 走在线 LLM 时才需要。
# 变量名【必须】是 BASE_URL / API_KEY（utils/openai_gpt.py 固定读这两个名，
# 不要写成 OPENAI_BASE_URL / OPENAI_API_KEY，否则不生效）
# BASE_URL=https://your-api.com/v1
# API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# ── 4. HuggingFace 镜像 / 离线（可选，国内用户友好）────────────
# HF_ENDPOINT=https://hf-mirror.com
# HF_HUB_OFFLINE=1
# TRANSFORMERS_OFFLINE=1
